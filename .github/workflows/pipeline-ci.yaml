name: Tests CI

on:
  push:
    branches:
      - main
      - 213-tasktestes-configurar-testes-estáticos-com-linter
  pull_request:
    branches:
      - main
  #workflow_dispatch:
    # Permite que o workflow seja disparado manualmente
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      db:
        image: postgres:15
        env:
          POSTGRES_DB: ${{ secrets.DB_NAME }}
          POSTGRES_USER: ${{ secrets.DB_USER }}
          POSTGRES_PASSWORD: ${{ secrets.DB_PASSWORD }}
        ports:
          - 5432:5432

      redis:
        image: redis
        ports:
          - 6379:6379

    env:
      CELERY_BROKER_URL: redis://localhost:6379/0
      CELERY_RESULT_BACKEND: redis://localhost:6379/0
      PYTHONPATH: ${{ github.workspace }}/API  # Caminho para o diretório API
      DJANGO_SETTINGS_MODULE: AcheiUnB.settings  # Ajustado para o módulo correto
      DB_HOST: localhost
      DB_PORT: 5432
      DB_NAME: ${{ secrets.DB_NAME }}
      DB_USER: ${{ secrets.DB_USER }}
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
      CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
      CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r API/requirements.txt
          pip install coverage
          pip install black ruff  # Garantir que black e ruff estão instalados

      - name: Run migrations
        run: python API/manage.py migrate

      - name: Run Black (formatação)
        run: black --check API/

      - name: Run Ruff (linting)
        run: ruff check API/

      - name: Post comment on PR if linting fails
        if: failure() && github.event_name == 'pull_request' # Adicionado condição para PRs
        uses: mshick/add-pr-comment@v2
        with:
          message: |
             ❌ **O código não passou na formatação e linting!**

             Por favor, corrija os problemas antes de fazer push novamente:

             🖊️ **Formatação:** Rode `black .`
             🔍 **Linting:** Rode `ruff check --fix .`

             Depois de corrigir, faça `git add .`, `git commit -m "Corrigindo lint"` e `git push`.

      - name: Run tests with coverage
        run: |
          coverage run -m pytest
          coverage report -m
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
    continue-on-error: true # Permite que o job 'test' não quebre a pipeline, mesmo que falhe.         

  security-scan:
    runs-on: ubuntu-latest
    needs: test # Garante que o estágio de segurança só roda após os testes
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python for security scan
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' # Use a mesma versão do Python do estágio de teste

      - name: Install security scanners (Bandit) # O Safety será instalado pela sua própria Action
        run: |
          pip install --upgrade pip
          pip install bandit # Instala apenas o Bandit aqui

      - name: Install project dependencies (for Safety CLI as well)
        run: |
          pip install -r API/requirements.txt # Garante que as dependências do projeto estão no ambiente para o Safety CLI

      - name: Run Bandit (SAST)
        run: |
          # O Bandit gera o relatório HTML no diretório raiz do workspace
          bandit -r API/ -f html -o bandit_security_report.html
          echo "Bandit scan completed. Report generated at bandit_security_report.html"
        continue-on-error: true # Permite que o workflow continue mesmo se o Bandit encontrar issues e retornar um código de erro

      # --- REMOVER OS PASSOS DE DEBUG DO SAFETY CLI (NÃO SERÃO NECESSÁRIOS COM A ACTION OFICIAL) ---
      # Remova os passos "Debug Network Connectivity to Safety Platform" e "Verify Safety CLI Authentication Status"
      # pois a pyupio/safety-action lida com a autenticação e conectividade internamente de forma mais otimizada.

      # --- NOVO PASSO: RUN SAFETY CLI (SCA) USANDO AÇÃO OFICIAL ---
      - name: Run Safety CLI (SCA) using official action
        uses: pyupio/safety-action@v1 # Usa a Action oficial para Safety CLI
        with:
          api-key: ${{ secrets.SAFETY_API_KEY }} # Passa a API Key diretamente para a Action
          # Argumentos para o safety scan, passados via 'args'
          args: "--full-report --output-format html --ci" # Mantemos --full-report e --output-format html e --ci
          # A Action pyupio/safety-action por padrão salva o relatório como 'safety-report.json'.
          # Precisamos configurá-la para HTML e dar o nome desejado.
          # A documentação da action indica que o --output-format html é suportado.
          # O nome do arquivo de saída padrão é 'safety-report.json'. Pode ser preciso renomear.
          # Vamos verificar a saída da Action para confirmar o nome do arquivo HTML.
        continue-on-error: true # Permite que o workflow continue mesmo se o Safety encontrar issues e retornar um código de erro

      - name: Rename Safety Report (if necessary)
        # Este passo pode ser necessário se a pyupio/safety-action não gerar o HTML com o nome exato.
        # Por padrão, safety-action gera 'safety-report.json'. Verifique os logs após a execução.
        # Se ela gerar 'safety-report.html', esta etapa não é estritamente necessária.
        # Se ela gerar JSON, você pode precisar converter ou aceitar JSON.
        run: |
          if [ -f safety-report.json ]; then
              # Se a action gera JSON por padrão, você pode querer converter para HTML
              # ou aceitar o JSON e mudar o path no upload-artifact
              echo "Safety Action generated JSON report. Please check its documentation for HTML output filename."
              # safety-action@v1 does support --output-format html directly in args.
              # So, it should generate safety-report.html if that is the case.
          fi
          # Renomeia se o nome padrão da action for diferente do que você quer upar
          mv safety-report.html safety_security_report.html || true # Assumindo que a action gera 'safety-report.html'
        continue-on-error: true # Para não quebrar a pipeline se a renomeação falhar.

      - name: Upload Security Reports
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: |
            bandit_security_report.html
            safety_security_report.html # VERIFIQUE O NOME QUE A ACTION safety-action GERA
          retention-days: 5
       